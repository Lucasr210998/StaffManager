<Window x:Class="StaffManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StaffManager"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeaderBarStyle}" Grid.Row="0">
            <TextBlock Text="Staff Manager" Style="{StaticResource HeaderTextStyle}" />
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Staff Name:" VerticalAlignment="Center" Margin="5"/>
                <TextBox x:Name="StaffNameTextBox" Width="200" Height="34" Margin="10" Grid.Row="1" FontSize="12" />
                <Button Content="Add Staff" Width="100" Height="30" Margin="10" Grid.Row="1" Command="{Binding AddPersonCommand}"
                    CommandParameter="{Binding Text, ElementName=StaffNameTextBox}"/>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="2"
                Height="auto">
            <ListBox x:Name="StaffListBox" Margin="10" ItemsSource="{Binding Persons}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Width="auto">
                            <TextBlock Text="{Binding Name}" FontSize="16" Margin="5"/>
                            <Button Content="Remove" Width="75" Height="25" Margin="5" Command="{Binding DataContext.RemovePersonCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" HorizontalAlignment="Right"/>
                            <Button Content="Edit"
                                    Width="75"
                                    Height="25"
                                    Margin="5"
                                    Command="{Binding DataContext.EditPersonCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
